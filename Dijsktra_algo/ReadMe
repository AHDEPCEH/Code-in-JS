Мельниченко Андрей КНМО-101
Постановка задачи:
На вход подаётся арифметическое выражение, 
которое необходимо преобразовать к обратной польской записи и вычислить её по алгоритму.
На вход программе подаётся, в качестве аргументов, название файла с примером и название файла для записи ответа.
В качестве ответа выводится результат подсчёта через польскую запись, а в следующей строке результат вычислений 
при помощи Eval(Встроенной функции в JS, которая возвращает результат работы, переданного, кода).
